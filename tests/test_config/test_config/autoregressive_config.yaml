model_type: "autoregressive"
module_config:
  img_channel: 3
  num_channels: 128
  num_layers: 3
data:
  batch_size: 64
  num_workers: 4
train:
  num_epochs: 10
  learning_rate: 0.001
  optimizer: "adam"
